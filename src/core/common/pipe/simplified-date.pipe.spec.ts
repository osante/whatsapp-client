import { SimplifiedDatePipe } from './simplified-date.pipe';

describe('SimplifiedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SimplifiedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
